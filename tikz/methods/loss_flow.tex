
\begin{tikzpicture}[
    font=\scriptsize,
    >=Stealth,
    % --- Main Node Style ---
    mainnode/.style={
        draw=navy, 
        fill=white, 
        thick, 
        rounded corners=3pt, 
        align=center, 
        text=navy, 
        minimum width=4.0cm, 
        minimum height=0.55cm, 
        font=\scriptsize\bfseries
    },
    bufferbox/.style={
        draw=amber, 
        fill=amber!5, 
        thick, 
        dashed, 
        rounded corners=3pt
    },
    buffernode/.style={
        draw=amber, 
        fill=amber!15, 
        thick, 
        rounded corners=2pt, 
        align=center, 
        font=\tiny\bfseries, 
        text=dark, 
        minimum width=1.8cm, 
        minimum height=0.5cm
    },
    arr/.style={-Latex, thick, draw=navy},
    dataarr/.style={-Latex, semithick, draw=teal},
    looparr/.style={-Latex, semithick, draw=navy!60, dashed},
    innerlooparr/.style={-Latex, semithick, draw=teal, dashed},
    outerbg/.style={draw=dark!25, fill=gray, dashed, rounded corners=4pt},
    innerbg/.style={draw=teal!35, fill=teal!5, dashed, rounded corners=4pt},
    tap/.style={circle, fill=teal, inner sep=0pt, minimum size=3pt}
]

    % ==========================================================================
    % 1. HINTERGRUND-ZONEN (Outer vs. Inner Loop)
    % ==========================================================================
    % Inner Loop Box
    \filldraw[innerbg] (-3.5, -2.8) rectangle (4.4, -6.6);
    \node[anchor=north west, font=\tiny\bfseries, text=teal!80!black] at (-3.5, -2.8) {Inner Epoch};

    % Outer Loop Box
    \filldraw[outerbg] (-3.7, 0.8) rectangle (5.3, -7.5);
    \node[anchor=north west, font=\tiny\bfseries, text=dark!70] at (-3.7, 0.8) {Outer Epoch};

    % ==========================================================================
    % 2. MAIN WATERFALL NODES (Spalte in der Mitte at x=0)
    % ==========================================================================
    \node[font=\tiny\bfseries, text=dark, draw=dark, fill=white, rounded corners=2pt, inner sep=2pt] (start_outer) at (0, 0.4) {Start Outer Epoch};

    \node[mainnode] (n1) at (0, -0.5) {1. $\rho$-Estimation};
    \node[mainnode] (n2) at (0, -1.3) {2. Adversarial Falsification};
    \node[mainnode] (n3) at (0, -2.1) {3. State Pool Resampling};
    
    \node[font=\tiny\bfseries, text=teal!90!black, draw=teal, fill=white, rounded corners=2pt, inner sep=2pt] (start_inner) at (0, -3.1) {Start Inner Epoch};
    
    \node[mainnode] (n4) at (0, -4.1) {4. Mini-Batch Assembly};
    \node[mainnode] (n5) at (0, -4.9) {5. Loss Computation};
    \node[mainnode] (n6) at (0, -5.7) {6. Optimization Step};

    % Hauptfluss-Pfeile
    \draw[arr] (start_outer) -- (n1);
    \draw[arr] (n1) -- (n2);
    \draw[arr] (n2) -- (n3);
    \draw[arr] (n3) -- (start_inner);
    \draw[arr] (start_inner) -- (n4);
    \draw[arr] (n4) -- (n5);
    \draw[arr] (n5) -- (n6);

    % ==========================================================================
    % 3. CEGIS BUFFER (Rechts oben, x = 3.2)
    % ==========================================================================
    \filldraw[bufferbox] (2.3, -0.8) rectangle (4.1, -2.6);
    \node[anchor=south, font=\tiny\bfseries, text=amber!80!black] at (3.2, -0.8) {CEGIS Buffer};
    
    \node[buffernode] (pool_c) at (3.2, -1.3) {Counterexample\\[-1pt]Pool $\mathcal{C}$};
    \node[buffernode] (pool_s) at (3.2, -2.1) {Explorative\\[-1pt]Pool $\mathcal{S}$};

    % Eingänge in den Puffer (horizontal, überschneidungsfrei)
    \draw[dataarr] (n2.east) -- (pool_c.west) node[midway, above, font=\tiny, text=teal] {$x_{\text{adv}}$};
    \draw[dataarr] (n3.east) -- (pool_s.west) node[midway, above, font=\tiny, text=teal] {$x$};

    % Ausgang aus dem Puffer (verlässt Box nach unten bei y = -3.5, unterhalb von start_inner!)
    \draw[dataarr, rounded corners=3pt] (3.2, -2.6) -- (3.2, -4.1) -- (n4.east);

    % ==========================================================================
    % 4. DATENBUSSE (Links)
    % ==========================================================================
    % Bus 1: Rho & ROA
    \draw[dataarr] ($(n1.west)+(0,0.08)$) -- (-2.5, -0.42);
    \node[anchor=east, font=\tiny\bfseries, text=teal] at (-2.5, -0.65) {$\rho, \mathcal{B}_{\text{ROA}}$};
    \draw[dataarr, rounded corners=2pt] (-2.5, -0.42) -- (-2.5, -4.82) -- ($(n5.west)+(0,0.08)$);
    
    % Abzweigungen für Rho
    \node[tap] at (-2.5, -1.3) {}; \draw[dataarr] (-2.5, -1.3) -- (n2.west);
    \node[tap] at (-2.5, -2.1) {}; \draw[dataarr] (-2.5, -2.1) -- (n3.west);

    % Bus 2: LIRPA
    \node[font=\tiny\bfseries, text=navy] (lirpa) at (-3.2, 0.4) {LIRPA};
    \draw[dataarr, rounded corners=2pt] (lirpa.south) -- (-3.2, -4.98) -- ($(n5.west)-(0,0.08)$);

    % ==========================================================================
    % 5. SCHLEIFEN-RÜCKFÜHRUNGEN (Rechts gestaffelt, ohne Kreuzung)
    % ==========================================================================
    % Inner Loop: Mündet genau auf Höhe y = -3.1 (oberhalb des Puffer-Ausgangs!)
    \draw[arr] (n6.south) -- (0, -6.3);
    \draw[innerlooparr, rounded corners=3pt] (0, -6.3) -- (4.7, -6.3) -- (4.7, -3.1) -- (start_inner.east);
    \node[fill=white, draw=teal!40, rounded corners=2pt, font=\tiny\bfseries, text=teal!90!black, inner sep=1pt] at (4.7, -4.9) {\rotatebox{90}{Inner Loop ($K$ steps)}};

    % Outer Loop: Führt weiter nach unten und außen herum zurück zu start_outer
    \draw[arr] (0, -6.3) -- (0, -7.1);
    \draw[looparr, rounded corners=3pt] (0, -7.1) -- (5.5, -7.1) -- (5.5, 0.4) -- (start_outer.east);
    \node[fill=white, draw=dark!25, rounded corners=2pt, font=\tiny\bfseries, text=dark, inner sep=1pt] at (5.5, -3.3) {\rotatebox{90}{Outer Loop ($N$ epochs)}};

\end{tikzpicture}
