\begin{tikzpicture}
  \begin{axis}[
        axis lines = middle,
        xlabel = {$z$},
        ylabel = {$a$},
        xmin = -3.5, xmax = 3.5,
        ymin = -1.5, ymax = 3.5,
        grid = major,
        grid style = {dashed, gray!30},
        legend pos = north west,
        legend style = {draw=none, fill=white, fill opacity=0.8, text opacity=1},
        tick label style = {font=\small},
        label style = {font=\small},
        width=10cm,
        height=8cm
  ]

    % ReLU
    \addplot[
        domain=-3.5:3.5,
        samples=100,
        color=amber,
        ultra thick
    ] {max(0, x)};
    \addlegendentry{$[z]_+$}
    
    % Leaky ReLU
    \addplot[
        domain=-3.5:3.5,
        samples=100,
        color=teal,
        ultra thick,
        dashed
    ] {max(0.1*x, x)};
    \addlegendentry{$[z]_{\alpha=0.1}$}

    % Tanh
    \addplot[
        domain=-3.5:3.5,
        samples=100,
        color=navy,
        ultra thick,
        dotted
    ] {tanh(x)};
    \addlegendentry{$\tanh z$}

  \end{axis}
\end{tikzpicture}